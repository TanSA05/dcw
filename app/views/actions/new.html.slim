h1.newC #{@action[:name].to_s.humanize} | Complaint ##{@complaint.complaint_number} by #{@complaint.complainant}

== render "complaints/partials/complaint_nav", current: "actions"

== render 'actions/action_form', action: @action